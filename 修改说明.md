修改说明汇总

更新时间: 2026-06-05

说明: 本文档记录每次对 `日程规划` 项目的代码修改要点与目的，便于审计与回滚。以后每次修改后都会追加。

v0.3.1 - 插件化兼容
- 新增 `src/services/plugin_manager.py`：实现插件扫描、加载、注册面板、启用/禁用逻辑。
- 在 `App` 启动流程中实例化 `PluginManager` 并加载插件（在 `DataManager` 初始化之后，`MainWindow` 创建之前）。
- 修改 `src/ui/main_window.py` 与 `src/ui/components/main_workspace.py`：添加 `plugin_manager` 参数并传递到设置面板。
- 修改 `src/ui/panels/settings_panel.py`：添加“插件”卡片，用于列出插件、切换启用/禁用、打开插件提供的面板。
- 新增示例插件 `plugins/pomodoro`（番茄钟），包含 `IsOpen.able` 与 `plugin.py`，用于验证插件加载与面板打开。
- 新增 `PLUGINS.md`：描述插件目录结构、API 与使用方法。
- 更新 `接口文档.md`：说明插件系统在启动流程中的位置与注意事项。

修复记录（2026-06-05）
- 修复 `SettingsPanel._open_plugin` 的两个问题：
  - 将 `widget.setAttribute(0x00000002, True)` 替换为 `widget.setAttribute(Qt.WA_DeleteOnClose, True)`，使用正确的 Qt 枚举类型，避免 TypeError。
  - 修正 `create_panel` 的参数：传入 `self._data_manager` 而非重复传入 `self._settings`。

后续建议:
- 将插件面板支持嵌入主工作区导航（当前为独立窗口）。
- 增加 `manifest.json` 支持与插件元数据展示（作者、版本、权限）。
- 添加“重载插件”按钮以支持开发时热重载。

v0.3.2 - 修复面板闪现并美化 UI（2026-06-05）
- 修复：点击“打开面板”窗口闪烁后消失的问题，原因是没有在主程序中保留插件窗口的 Python 引用，导致 GC 回收并关闭窗口。解决方案：在 `SettingsPanel` 中增加 `self._open_plugin_windows` 列表以保存打开的窗口引用，并在窗口销毁时移除引用。
- 美化：调整主题与样式以提升可读性与审美：
  - 增大基础字体与标题：`SIZE_BODY`、`SIZE_BODY_L`、`SIZE_TITLE`、`SIZE_HEADING`。
  - 调整强调色与高亮（`accent` -> `#0f62fe`），使 UI 更清晰。
  - 稍微加深卡片背景并增强卡片边框可见度（`card_border`），让卡片更分层。
  - 使节标题颜色更醒目（`section_heading` 颜色调整）。
  - 在设置页的插件列表中为每个插件添加卡片样式、改进按钮样式和字号，使插件管理界面更友好。

影响文件：
- `src/ui/panels/settings_panel.py`：保持插件窗口引用、插件行样式优化。
- `src/ui/styles/theme.py`：字体大小与配色调整。

下一步建议：将插件面板支持嵌入主工作区并在设置页显示 `manifest.json` 元数据。

v0.3.4 - 结构规范与性能优化（2026-06-05）
- 启动顺序与加载规范：在 `接口文档.md` 中补充并具体化启动顺序、主题切换、插件延迟加载等开发流程规范，避免加载顺序错误与全局样式不同步的问题。
- 插件延迟加载：修改 `src/services/plugin_manager.py`，将插件模块导入从启动时移至按需（打开面板时）进行，减少启动内存占用和导入时间。
- 主题切换修复：修改 `MainWindow._switch_theme`，样式表现在通过 `QApplication.instance().setStyleSheet(qss)` 应用到全局，且会调用 `MainWorkspace.refresh_theme()` 与 `AIPanel.refresh_theme()` 等面板刷新样式，解决切换主题时局部窗口样式不同步的问题。
- 增加 `MainWorkspace.refresh_theme()`：统一对内嵌面板做主题刷新调用。

影响文件：
- `src/services/plugin_manager.py`（延迟导入与模块缓存）
- `src/ui/main_window.py`（主题切换改为作用于 QApplication 并刷新面板）
- `src/ui/components/main_workspace.py`（增加 refresh_theme）
- `接口文档.md`（新增启动顺序与性能/内存优化指南）

注意：这些更改以减少启动时内存与防止全局状态不同步为目标；后续若需要更严格的插件沙箱或线程隔离，可在此基础上扩展。

v0.3.5 - 主题刷新支持（2026-06-05）
- 为主要面板添加 `refresh_theme()` 接口并在主题切换时调用，确保全局主题切换后所有局部样式生效：
  - `src/ui/panels/course_panel.py`（新增 `refresh_theme`）
  - `src/ui/panels/task_panel.py`（新增 `refresh_theme`）
  - `src/ui/panels/habit_panel.py`（新增 `refresh_theme`，通过重建卡片应用新主题）
  - `src/ui/panels/expense_panel.py`（新增 `refresh_theme`）
  - `src/ui/widgets/schedule_view.py`（新增 `refresh_theme`，重渲染日程视图）
  - `src/ui/widgets/course_detail_dialog.py`（新增 `refresh_theme`）
  - `src/ui/panels/settings_panel.py`（新增 `refresh_theme`，刷新输入/下拉与插件列表样式）

影响与注意事项：
- 部分面板在 `refresh_theme()` 中选择重建视图（如 `HabitPanel`、`ScheduleView`）以保证所有内联样式使用最新颜色，这会在主题切换时短暂触发 UI 重绘，但可靠性更高。
- 下一步可把每个面板对局部控件的样式集中到少数方法，以便在 `refresh_theme()` 中只更新必要样式，减少重绘成本。

v0.3.3 - 在顶部导航添加“小功能”并应用全局美化（2026-06-05）
- 新增：`src/ui/panels/plugins_panel.py` — 在主工作区内展示插件列表，可启用/禁用并打开插件面板。
- 修改：将顶部导航增加 `小功能`（`widgets`）标签，点击后在主工作区显示 `PluginsPanel`（避免插件仅通过设置页访问）。
- 修改：将导航栏高度从 `46` 增至 `50`，并对主工作区堆栈索引做相应调整，避免文字或控件被遮挡。
- 已把插件管理界面在设置页和导航页都进行了样式美化，确保文字使用 `word-wrap` 并设置最小宽度以防止被截断。

影响文件：
- `src/ui/main_window.py`：导航项与高度调整。
- `src/ui/components/main_workspace.py`：新增 `PluginsPanel` 到堆栈并更新 `switch_to` 映射。
- `src/ui/panels/plugins_panel.py`：新增文件，展示插件列表并支持打开窗口。


