# 修改说明

## v0.9.0 (2026-06-09)

**性能优化**
- 启动提速：show() 提前到 MainWindow 创建后，tray 在窗口可见前就绪
- 日程视图增量更新：hash 跳过未变卡片，避免全量 deleteLater + insertWidget
- AI 指令内存调用：actions.run_in_process() 替代 subprocess，消除进程启动开销
- 窗口默认大小 1100×720 → 1210×792 (+10%)

**物品库存 (有什么)**
- 新增 inventory model + panel，5 大分类（日常消耗品/数码电子/衣物/虚拟品类/其他）
- 分类从 8 个精简到 5 个（日用品+食品+学习+药品→日常消耗品）
- 虚拟品类：话费/会员/API 额度等，DeepSeek 余额自动查询 (5min 缓存)
- 移除手动添加功能，全部依靠 AI 对话管理
- 右键菜单新增修改标签（多标签并存，双击编辑，添加/删除）
- 分类筛选从 model.categories 读取（修复只显示"其他"的 bug）
- delete_item 命令从零补齐（函数 + 两处 handler + SYSTEM_PROMPT）

**AI 增强**
- Agent Loop 信号链修复：finished_with_instructions 从未 emit 导致调整按钮挂死
- 调整 prompt 格式统一：JSON → ```sh (与 SYSTEM_PROMPT 一致)
- response_received 路径加 _arrange_pending 兜底检查

**文档体系**
- AGENTS.md：开发流程分档（L1/L2/L3 可 grep 条件）+ 改前/改后模拟规则
- ERRORS.md：索引+条目格式，3 条已记录
- dev-logs/：按日期文件平铺
- ATF 文档全量更新（signal/术语/决策/禁止项同步）

## v0.8.2 (2026-06-08)
- 清理死代码 (context_sidebar/function_tabs/hermes_life, -665行)
- ClickLabel 提取为公共组件

## v0.8.1 (2026-06-07)
- 考试卡片 UI 重构 (圆角卡片+倒计时徽章)
- 状态栏精简

## v0.7.1 (2025-06-06)

**Bug 修复**
- 任务 ID 重复：add() 改为遍历已有 ID 找空号，不再用 len(tasks)+1
- 任务复选框回环：toggled→clicked，避免 setChecked 触发二次标记
- DDL 计数不更新：reminder_service 监听 data_changed，"task" 变更时实时重算
- 日常 describeSchedule 函数恢复

**数据修复**
- 清理 tasks.json 中 3 个重复 ID (t_006 ×2, t_007 ×3)
- 典籍英译任务 ID 修正

## v0.7.0

**布局重构**
- 废弃横排导航，改为手风琴侧边栏 (180px) + 内容区
- 三角箭头 CSS 展开动画
- 侧边栏灰色 (#D0CCC4) 与内容区暖纸白分层

**设计系统**
- 暖纸书房全面落地：配色 / 字体 / 间距 / 圆角 / 阴影
- 深色模式完整色系
- 卡片从 QFrame→QWidget (解决圆角 artifact)

**日常面板**
- 卡片重设计：tag + 描述 + 操作按钮
- 三种周期：每日 (多次) / 每周几 / 每月 N 次
- 日次习惯显示连续天数，周次只显示星期
- 完成态卡片变淡

**课程表**
- 暖纸淡色块替代五彩颜色
- 单双周自动过滤

**AI**
- system prompt 格式约束 (禁换行、禁 Markdown)
- 发送后即时显示 "AI 正在准备回答…"
- 日程快速调整按钮

**状态栏**
- 增加 "N/N 项日常"
- 增加购物需求提示
- DDL 红标实时刷新

## v0.4.0

- 暖纸白 + 琥珀金设计令牌
- 全量去硬编码颜色
- QFrame→QWidget 卡片迁移
- 间距 / 圆角统一
