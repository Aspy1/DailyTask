AllTheFiles.md
微调建议：

[数据路径] 章节的表述优化

原文：

若检测到 mtime变化且差值 > 100ms，则判定为外部修改...

建议修改为：

若检测到 mtime变化且差值 ≥ 0.1 秒 (100ms)，则判定为外部修改...
理由：代码中使用的是 >= 0.1，文档中写成 > 100ms虽然意思相近，但为了绝对精确，建议保持一致，防止 AI 在编写 actions.py时因为边界值（刚好 100ms）而产生困惑。
[索引] 章节的状态同步

注意到 src/models/course.py等文件在索引中仍标记为 —，而在底部的“文件详情”中已经标记为 ✅。

建议：将顶部的索引也更新为 ✅，保持全文一致。



course_panel.py.md
缺失点：

术语映射：文档中仍为 *（待补）*
| 用户用语 | 代码/内部术语 | 所在位置 |
|---|---|---|
| 课程表 | Course Table / 课程面板 | `course_panel.py` |
| 周次 | Week Number | `_week_number` 属性 |
| 学期 | Semester | `models/course.py` |


exam_panel.py.md
| 用户用语 | 代码/内部术语 | 所在位置 |
|---|---|---|
| 考试 | Exam | `exam_panel.py` |
| 紧迫度 | Urgency / days_left | `_ExamCard` |
| 考试范围 | Scope | `models/exam.py` |
expense_panel.py.md
#### 禁止做的事
- 禁止在 `_get_filtered` 中修改原始数据列表，必须返回副本。
- 禁止硬编码分类名称（如 "餐饮"），必须与 `models/expense.py` 中的定义保持一致。
plugins_panel.py.md
缺失点：

信号：文档中仍为 *（待补）*。虽然代码中没有 Qt 信号，但文档需要明确说明“无 Qt 信号，依赖 PluginManager的回调”。

术语映射：文档中仍为 *（待补）*。
#### 信号
*   无 Qt 信号。插件窗口的生命周期由 `PluginManager` 和 `self._open_plugin_windows` 列表管理。

#### 术语映射
| 用户用语 | 代码/内部术语 | 所在位置 |
|---|---|---|
| 插件 | Plugin | `plugins_panel.py` |
| 启用/禁用 | Enable / Disable | `plugin_manager.py` |