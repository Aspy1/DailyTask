# 生活助手 — 项目总结与展望

> 版本 0.3.0 | 2026-06-05

## 一、项目概述

生活助手是一款 AI 驱动的 PySide6 桌面日程管理应用，面向高校学生的课程、作业、习惯、记账、余额等日常管理需求。用户通过自然语言与 AI 交互，AI 自动将意图转化为结构化数据操作。

## 二、现有功能清单

### 学习模块
| 功能 | 状态 | 说明 |
|------|------|------|
| 日程视图 | 已完成 | 按时段展示当日课程、自定义计划、习惯，左侧 ▶ 指示当前时间 |
| 课程表 | 已完成 | 周视图网格，支持多时段、周段解析、教师轮换，右键重命名/删除 |
| 作业管理 | 已完成 | 表格视图，按课程/时间筛选，复选框完成，DDL 自动提醒 |
| 调休/假期 | 已完成 | AI 识别调休通知，假期不显示课程，调休日映射到指定星期课表 |

### 生活模块
| 功能 | 状态 | 说明 |
|------|------|------|
| 生活习惯 | 已完成 | 卡片网格布局，支持每周固定日 / 每隔 N 天，附加事项自动生成待办 |
| 记账 | 已完成 | 分类支出、月度预算、今日/本月/剩余三卡片，校园卡/水卡自动扣减余额 |
| 存钱助手 | 已完成 | 记录每日省钱金额，月度汇总 |
| 余额追踪 | 已完成 | 校园卡/水卡/电费余额低于阈值托盘弹窗 + 邮件提醒 |

### AI 助手
| 功能 | 状态 | 说明 |
|------|------|------|
| 自然语言操作 | 已完成 | 支持 30+ 种数据操作：增删改课程/作业/习惯/支出/余额/日程 |
| 关键词分流 | 已完成 | 老师布置→作业、我习惯→习惯、提醒我→自定义日程 |
| 双后端 | 已完成 | DeepSeek API + Claude API，设置中切换 |
| 上下文压缩 | 已完成 | 超过 16 条消息自动压缩，保留关键信息 |
| 聊天气泡 | 已完成 | Markdown 渲染、用户蓝色右对齐、AI 深色左对齐 |

### 提醒系统
| 功能 | 状态 | 说明 |
|------|------|------|
| 习惯提醒 | 已完成 | 提前 5 分钟托盘弹窗，漏检启动补发 |
| DDL 紧急提醒 | 已完成 | 截止前 6 小时邮件，3 天内红点 + 托盘闪烁 |
| 考试提醒 | 已完成 | 考前 14/7/3 天邮件 + 托盘 |
| 余额提醒 | 已完成 | 每日一次托盘 + 邮件 |
| 自定义邮件提醒 | 已完成 | 右键日程项自由设定提醒时间，本机 SMTP 发送 |
| 每日摘要 | 已完成 | GitHub Actions 04:55 北京时间发送，含课程/计划/习惯/DDL/考试/余额 |

### 系统特性
| 功能 | 状态 | 说明 |
|------|------|------|
| 系统托盘 | 已完成 | 最小化到托盘，右键菜单退出/同步 |
| 开机自启动 | 已完成 | Windows 注册表写入 |
| 单实例锁 | 已完成 | QLocalServer 防止多开 |
| 深色/浅色主题 | 已完成 | QSS 动态切换，右键托盘切换 |
| Git 自动同步 | 已完成 | 30 秒防抖自动 commit + 手动同步按钮 |
| 原子写入 | 已完成 | .tmp → os.replace() + 5 次 PermissionError 重试 |
| 刷新重载 | 已完成 | 点击刷新从磁盘重新加载 JSON |
| 自定义图标 | 已完成 | 48x48 像素图，AppUserModelID 任务栏识别 |

## 三、开发中遇到的问题与解决

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| settings.ini 不持久化 | `_apply_defaults()` 无条件覆盖所有键 | 改为 `if not has_option` 仅填充缺失项 |
| AI 聊天黑背景 | QTextEdit 在 Windows 上忽略 QSS 背景色 | 替换为 QScrollArea + QLabel (HTML) |
| SpinBox 数字乱码 | QSpinBox 使用 Windows 原生控件渲染异常 | 替换为可编辑 QComboBox |
| 右键菜单深色主题白底 | QSS 颜色硬编码 | 运行时调用 `get_colors()` 动态获取 |
| Enter 键不发送消息 | 未覆盖 QTextEdit 的 keyPressEvent | 自定义 `_ChatInput` 捕获 Enter |
| 程序双开 | QLocalServer 竞态条件 | `removeServer()` → `listen()` 检查 → 失败即退出 |
| 原子写入 PermissionError | Windows Defender/索引器短暂锁文件 | 5 次重试 + 50ms 间隔 |
| 习惯页面双击后消失 | `deleteLater()` 后 `_layout_children` 遍历到已删除子控件 | `setParent(None)` + `hide()` + `deleteLater()` |
| 日程右键菜单位置相同 | lambda 闭包捕获了循环变量的最终值 | 所有菜单改用 `QCursor.pos()` 直接获取 |
| add_reminder 写入作业 | 执行器错误调用 `dm.tasks.add()` | 改为 `dm.daily_logs.add_plan()` 写自定义日程 |
| 程序运行时手动改 JSON 被覆盖 | 内存数据与磁盘不一致，保存时回写 | 刷新按钮增加 `reload_all()`，程序外修改需关闭程序 |

## 四、原始计划完成度

对照 `概述.md` 中的原始需求：

| 需求 | 完成 | 备注 |
|------|------|------|
| i18n 语言文件分离 | ✓ | zh-CN.json，loader.py |
| 深度 AI 集成 | ✓ | 30+ 种数据操作，自然语言驱动 |
| Qt UI (非命令行) | ✓ | PySide6，PCL 风格顶部导航 |
| DeepSeek + Claude 双后端 | ✓ | 设置中切换 |
| 课程表 + 上课提醒 | ✓ | 周视图 + 时段日程 |
| 日历 | 部分 | 日视图已实现，缺月/周日历视图 |
| 记账 + 分类 + 时间筛选 | ✓ | 6 大分类，月度预算 |
| 生活习惯 | ✓ | 每周固定日 / 间隔 N 天 |
| 数据分离 (JSON/INI) | ✓ | 5 个 JSON 文件 + settings.ini |
| VSCode 式布局 | ✓ | 已演化为 PCL 风格顶部导航 + 子标签 |
| 托盘 + 自启动 | ✓ | 系统托盘右键菜单，注册表自启 |
| 定时提醒 | ✓ | 习惯/DDL/考试/余额 + 自定义 |
| 校园卡提醒 | ✓ | 余额追踪 + 支付自动扣减 |
| AI 上下文保存/压缩 | ✓ | 16 条消息自动压缩 |

**未完成项**：
- 月/周日历视图（仅有日日程视图）
- 多设备数据同步方案（内网穿透/P2P）尚未实施
- 安卓迁移尚未开始

## 五、后续规划

### 近期（v0.4 — v0.5）

**数据分析面板** — 学习/生活模块各增加一个"统计"子标签：
- 支出饼图 + 月度趋势折线图
- 习惯完成率热力图（类似 GitHub 贡献图）
- 作业按时完成率

**日历视图** — 月/周日历组件，标记有课/有作业/有考试的日期，点击跳转到当日日程。

**成绩管理** — 新增"成绩"面板：课程 → 成绩录入 → GPA 自动计算，支持学分加权。

**番茄钟** — 独立的倒计时工具，关联课程/作业，记录专注时长并汇总到统计面板。

### 中期（v0.6 — v0.7）

**数据同步服务** — 实现方案 1（内网穿透 + 账号系统）：
- 本机启动轻量 HTTP 服务（FastAPI）
- SQLite 替代 JSON 作为服务端存储
- 账号注册/登录，数据上云
- frp/nps 内网穿透方案

**插件系统** — 允许自定义面板以 Python 脚本形式载入，规范接口：`register_panel(name, widget_class)`。

**Web 管理端** — 浏览器访问本地端口查看/编辑数据，方便在手机上使用。

### 远期（v0.8+）

**安卓迁移** — 核心数据模型已与 UI 分离（model 不依赖 Qt），可复用：
- 前端：Kotlin/Jetpack Compose
- 后端逻辑：Python 模型层通过 Chaquopy 或重写为 Kotlin 数据类
- 同步：连接桌面端同步服务

**智能排程** — AI 根据未完成任务 + 习惯 + 空闲时段，自动建议今日计划。

## 六、技术架构

```
main.py (入口)
  └── src/app.py (QApplication, 托盘, 单实例, 自启动)
        └── src/ui/main_window.py (顶部导航 + 状态栏)
              ├── src/ui/components/main_workspace.py (标签页切换)
              │     ├── ScheduleView (日程, 默认)
              │     ├── CoursePanel (课程表)
              │     ├── TaskPanel (作业)
              │     ├── HabitPanel (习惯卡片)
              │     ├── ExpensePanel (记账)
              │     └── SettingsPanel (设置)
              └── src/ui/components/ai_panel.py (AI 聊天)

数据层 (不依赖 Qt):
  src/models/
    ├── base.py          (BaseJsonModel: 原子写入, 版本迁移)
    ├── course.py        (课程, 学期, 假期/调休, schedule_date)
    ├── task.py          (作业, DDL, 优先级)
    ├── habit.py         (习惯, 周期, 附加事项, 推迟/取消)
    ├── expense.py       (支出, 分类, 预算)
    └── daily_log.py     (日程计划, 省钱, 自定义提醒)

服务层:
  src/services/
    ├── data_manager.py  (数据中枢, reload_all, 信号)
    ├── ai_service.py    (DeepSeek/Claude, 指令解析, 上下文压缩)
    ├── reminder_service.py (定时检查, 邮件/托盘, catch-up)
    ├── git_sync.py      (30s 防抖自动 commit)
    └── settings_manager.py (INI 读写, API Key 管理)
```

## 七、致谢

本项目在 UI 设计上参考了以下开源项目：

- **Plain Craft Launcher (PCL)** — 顶部导航栏的分组标签设计（学习/生活/设置/更多）参考了 PCL 的首页导航布局，其清晰的视觉分组和状态栏信息展示方式对本项目的 UI 结构有重要启发。
- **Visual Studio Code** — 早期原型阶段参考了 VS Code 的多列布局（功能图标栏 + 侧边栏 + 工作区 + AI 面板），虽然后续演化为 PCL 风格的顶部导航，但其工作区与面板分离的设计思想贯穿始终。

AI 对话与指令解析受 Claude Code 的工具调用模式启发——自然语言 → 结构化 JSON → 程序执行。
