# 生活助手 — 代码结构接口文档

> 最后更新: 2026-06-05
> 此文档记录类之间的依赖关系、初始化顺序、信号连接和常见崩溃点。
> 修改任何涉及多文件调用的代码前，先读此文档。

## 一、启动流程（app.py）

```
App.__init__
  ├─ SettingsManager(settings_dir)           # 读取 settings.ini
  ├─ DataManager(data_dir)                   # 加载全部 JSON → 内存
  │    ├─ courses.json  → CourseModel
  │    ├─ tasks.json    → TaskModel         # __init__ 触发 _auto_archive()
  │    ├─ habits.json   → HabitModel
  │    ├─ expenses.json → ExpenseModel
  │    └─ daily_logs.json → DailyLogModel
  ├─ AIService(settings, data_manager)       # 不触发网络调用
  ├─ ReminderService(settings, data_manager) # 只初始化定时器，不 emit
  │    ├─ 连接信号:
  │    │   reminder_notify → _show_reminder_notify  (托盘弹窗)
  │    │   ddl_alert       → _on_ddl_alert          (状态栏红标)
  │    │   ⚠ start() 必须在 _window 创建之后调用
  │    └─ start() 后:
  │         ├─ _catch_up()       → 补发错过的习惯提醒 → 可能 emit reminder_notify
  │         ├─ _check_ddl()      → emit ddl_alert(count)
  │         └─ _check_balances() → 余额不足 → emit reminder_notify + 发邮件
  ├─ GitSync(project_dir, data_dir)
  ├─ MainWindow(settings, ai_service, data_manager)
  │    ├─ _setup_ui()          # 导航栏 + 工作区 + AI面板
  │    ├─ _setup_statusbar()   # 初始化 _ddl_alert_label (隐藏)
  │    ├─ _connect_ai()        # AI消息信号
  │    ├─ _connect_course_table()
  │    └─ _refresh_status()    # 首次刷新状态栏（日期/课程/支出）
  └─ _setup_tray()             # 系统托盘
```

### ⚠ 关键初始化顺序约束

| 顺序 | 约束 | 违反后果 |
|------|------|----------|
| DataManager 先于 AI/Reminder | AI/Reminder 构造时读取 data_manager 数据 | AttributeError |
| 信号 connect() 先于 start() | start() 内的 emit 需要已有监听者 | 信号静默丢失 |
| _window 先于 reminder.start() | _on_ddl_alert 调用 self._window.set_ddl_alert() | **AttributeError: 'App' object has no attribute '_window'** |
| MainWindow 先于 QSystemTrayIcon | 托盘图标需要窗口引用 | 托盘无响应 |

## 二、信号流向

```
DataManager
  └─ data_changed(action: str)
       ├─→ GitSync.on_data_changed()    # 30s 防抖自动 commit
       └─→ MainWorkspace._on_data_changed() → 各面板 _refresh()

ReminderService
  ├─ reminder_notify(title, body)
  │    └─→ App._show_reminder_notify()  → QSystemTrayIcon.showMessage()
  └─ ddl_alert(count: int)
       └─→ App._on_ddl_alert()
            └─→ MainWindow.set_ddl_alert(count)
                 └─→ _ddl_alert_label.show() / hide()

AIService
  ├─ response_received(text)
  │    └─→ AIPanel.append_message_ai(text)
  ├─ error_occurred(msg)
  │    └─→ AIPanel._on_response_done()
  └─ instructions_executed(text, instructions)
       └─→ AIPanel._on_response_done()

AIPanel
  └─ message_sent(text)
       └─→ MainWindow._on_ai_message()

ScheduleView
  ├─ course_table_requested
  │    └─→ MainWorkspace._switch_study_sub("courses")
  └─ task_focus_requested(course_name)
       └─→ MainWorkspace._on_task_focus()
            └─→ TaskPanel.filter_by_course(course_name)
```

## 三、类依赖矩阵

| 类 | 依赖 | 不可为 None |
|----|------|-------------|
| ReminderService | SettingsManager, DataManager | 两个都是 |
| AIService | SettingsManager, DataManager | 两个都是 |
| MainWindow | SettingsManager, AIService, DataManager | 三个都是 |
| MainWorkspace | SettingsManager, AIService, DataManager | 三个都是 |
| ScheduleView | DataManager | ✓ |
| TaskPanel | DataManager | ✓ |
| CoursePanel | DataManager | ✓ |
| HabitPanel | DataManager | ✓ |
| ExpensePanel | DataManager | ✓ |
| SettingsPanel | SettingsManager, AIService | ✓ |
| AIPanel | 无 | — |
| GitSync | project_dir, data_dir | ✓ |

## 四、DataManager 模型访问

```
DataManager
  ├─ .courses      → CourseModel (courses.json)
  ├─ .tasks        → TaskModel   (tasks.json)
  ├─ .habits       → HabitModel  (habits.json)
  ├─ .expenses     → ExpenseModel (expenses.json)
  └─ .daily_logs   → DailyLogModel (daily_logs.json)

每个 Model:
  ├─ ._data        → 内存中的 dict（所有修改直接改这里）
  ├─ .save()       → atomic_write 到磁盘（.tmp → os.replace）
  ├─ .load()       → 从磁盘重新读取 → 覆盖 _data
  └─ .reload()     → 同 load() 的别名

DataManager:
  ├─ .save_all()   → 保存全部模型
  └─ .reload_all() → 从磁盘重新加载全部模型
```

## 五、常见修改导致崩溃的场景

### 1. 在 app.py 中调整初始化顺序

任何重新排序都必须遵守第一节的约束表。尤其是：
- **ReminderService.start()** 必须在 **MainWindow 创建之后**
- **信号 connect()** 必须在 **start() 之前**

### 2. 修改 ReminderService 的初始检查

`start()` → `_catch_up()` / `_check_ddl()` / `_check_balances()` 中的任何一个 emit 信号时，确认对应的 handler 所需对象已存在：
- `_check_ddl()` → `ddl_alert` → `set_ddl_alert()` → 需要 `_window._ddl_alert_label`
- `_catch_up()` → `reminder_notify` → 需要 `QSystemTrayIcon`（在 `_setup_tray()` 中创建）
- `_check_balances()` → `_send_email()` → 需要 `SettingsManager` 中的 SMTP 配置

### 3. 在 MainWindow 中添加新控件

- 控件在 `_setup_ui()` / `_setup_statusbar()` 中创建
- 如果在 `set_ddl_alert()` 或 `_refresh_status()` 中引用，确保控件先于调用者创建
- `_ClickLabel.clicked` 信号只能在控件创建后 connect

### 4. 修改 TaskPanel 表格渲染

- 普通文本: `QTableWidgetItem` + `setItem(row, col)`
- 富文本(混色): `QLabel` + `setCellWidget(row, col)` → 必须同时渲染该行的所有其他列
- 使用 `setCellWidget` + `continue` 时，标题列和复选框列需要在该分支内手动创建
- 列宽不足会截断 QLabel 内容；富文本不加粗体

### 5. 编辑 JSON 数据文件

- **程序运行时绝对不能编辑 JSON**：内存 `_data` 会在下次 save 时覆盖手动修改
- 正确流程：关闭程序 → 编辑 JSON → 验证 → 启动程序

## 六、数据修改入口汇总

| 操作 | 入口 | 触发 save |
|------|------|-----------|
| AI 指令 | `InstructionParser.execute()` → model 方法 → `dm.save_all()` | 是 |
| UI 交互 | 各面板直接调用 model 方法 → `.save()` | 是 |
| 定时器 | `ReminderService._check()` → 只读 | 否 |
| Git sync | `GitSync.on_data_changed()` → `git add/commit/push` | 否 |
| 手动编辑 | 直接写 JSON 文件 | 需手动 |
| 刷新按钮 | `ScheduleView._refresh()` → `dm.reload_all()` | 否 |
