# 接口文档

## 数据模型 API

### TaskModel (tasks.json)

| 方法 | 说明 |
|------|------|
| `add(task: dict) → str` | 新增作业，自动生成唯一 ID |
| `complete(task_id) → bool` | 标记完成 |
| `uncomplete(task_id) → bool` | 撤销完成 |
| `update(task_id, changes) → bool` | 更新字段 |
| `delete(task_id) → bool` | 删除 |
| `update_notes(task_id, notes) → bool` | 更新备注 |
| `pending → list[dict]` | 待完成作业 (status=pending) |
| `completed → list[dict]` | 已完成作业 |
| `get_overdue() → list[dict]` | 逾期作业 |

### HabitModel (habits.json)

| 方法 | 说明 |
|------|------|
| `add_habit(habit) → str` | 新增日常 |
| `update_habit(id, changes) → bool` | 更新 |
| `delete_habit(id) → bool` | 删除 |
| `log(id, completed, note="") → list` | 打卡 (返回附加事项) |
| `is_active_today(id) → bool` | 今天是否激活 (含月次/周次/日次) |
| `is_done_today(id) → bool` | 今天是否已完成 |
| `postpone(id, until) → bool` | 推迟到指定日期 |
| `cancel_today(id) → bool` | 取消今天 |
| `habits → list[dict]` | 全量日常 |
| `logs → dict` | 打卡日志 |

### CourseModel (courses.json)

| 方法 | 说明 |
|------|------|
| `add_course(course) → str` | 新增课程 |
| `update_course(id, changes) → bool` | 更新 |
| `delete_course(id) → bool` | 删除 |
| `get_courses_for_date(date) → list` | 指定日期课程 (含单双周/假期) |
| `get_today_courses() → list` | 今日课程 |
| `add_holiday(date, label, makeup=None)` | 添加假期/调休 |

### ExpenseModel (expenses.json)

| 方法 | 说明 |
|------|------|
| `add(record)` | 新增支出 |
| `delete(id)` | 删除 |
| `today_total() → float` | 今日合计 |
| `month_total() → float` | 本月合计 |
| `records → list[dict]` | 支出记录 |
| `budget → dict` | 预算设置 |
| `categories → list[dict]` | 分类列表 |

### DailyLogModel (daily_logs.json)

| 方法 | 说明 |
|------|------|
| `add_plan(day, plan)` | 添加日程 |
| `delete_plan(day, index)` | 删除日程 |
| `get_plans(day) → list` | 获取日程 |

## AI 指令格式

AI 服务通过 system prompt 定义操作指令，返回结构化 JSON：

```json
{"action": "add_task", "data": {"title": "作业名", "course_name": "课程", "due_date": "2026-06-07T23:00"}}
{"action": "add_habit", "data": {"name": "习惯名", "schedule": {"type": "daily", "times_per_day": 1}}}
{"action": "log_habit", "data": {"habit_id": "h_001", "completed": true}}
{"action": "add_expense", "data": {"amount": 24.5, "category": "餐饮", "note": "午餐"}}
```

## 信号

| 信号 | 触发 |
|------|------|
| `data_changed.emit(action)` | 数据变更，action 为 task/habit/expense 等 |
| `ddl_alert.emit(count)` | DDL 紧急计数 (3天内) |
| `reminder_notify.emit(title, msg)` | 托盘提醒 |
