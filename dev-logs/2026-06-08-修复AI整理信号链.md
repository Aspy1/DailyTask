# 2026-06-08 修复AI整理信号链

## 涉及文件
- `src/services/ai_service.py` (Worker.run, _on_response_with_instructions)
- `src/ui/widgets/schedule_view.py` (_quick_arrange prompt)
- `src/ui/main_window.py` (_connect_ai, _on_ai_response)

## 问题描述
点击日程视图"调整"按钮 → 按钮变"..."且 disabled → AI 确实修改了数据 → 但按钮永不恢复，撤销/保存按钮不出现。前两次用户未察觉（数据确实变了），第三次意识到按钮缺失。

## 变更清单

### ai_service.py
| 位置 | 变更 |
|---|---|
| Worker.run() L134 | +`all_instructions = []` 追踪多轮指令 |
| Worker.run() L144-147 | 无指令时检查 `all_instructions` 非空 → emit `finished_with_instructions` |
| Worker.run() L148 | +`all_instructions.extend(instructions)` |
| Worker.run() L155-159 | 满轮次后检查 `all_instructions` → emit 对应信号 |
| _on_response_with_instructions L430-442 | 删除 `InstructionParser.execute()` 调用（Worker 已执行），只做通知 |

### schedule_view.py
| 位置 | 变更 |
|---|---|
| _quick_arrange L282-287 | JSON 格式 prompt → ```sh + python scripts/actions.py 格式 |

### main_window.py
| 位置 | 变更 |
|---|---|
| _connect_ai L266-267 | `response_received` 连接目标从 `ai_panel.append_message_ai` → `self._on_ai_response` |
| +_on_ai_response (新方法) | 检查 `_arrange_pending`，若 True 则清标记、调 `on_arrange_done(True, ...)`，再转发到 ai_panel |

## 关联
- ERRORS.md #001
