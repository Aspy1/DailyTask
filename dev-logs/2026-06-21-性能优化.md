# 2026-06-21 性能优化（从 dev 分支合并）

## 涉及文件
- `src/models/habit.py`
- `src/ui/panels/task_panel.py`
- `src/ui/styles/theme.py`

## 变更清单

### P1: habit.py O(n²)→O(1)
- 删除重复的 `get_pending_today` 定义
- 添加 `_habit_by_id` 索引字典
- `is_active_today()`/`log()`/`_update_streak()` 改为 O(1) 查找
- 预估提升：100 个习惯时 ~100×

### P2: task_panel.py QSS f-string 修复
- `_btn_qss()` 返回普通字符串 → 改为 f-string
- `{c["btn_secondary_bg"]}` 原样输出 → 正确解析为色值

### 设计令牌补充
- 新增 `course_color_1~8` 课程颜色令牌
- 新增 `btn_pressed_bg` 按钮按下状态令牌
- 新增 `badge_bg`/`badge_fg` 徽章令牌
